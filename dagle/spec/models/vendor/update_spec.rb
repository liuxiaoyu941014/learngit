require 'rails_helper'

RSpec.describe Vendor::Create, type: :model do

  # it 'for name' do
  #   record = create(:vendor)
  #   flag, new_record = Vendor::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:vendor)
  #   record2 = create(:vendor2)
  #   attrs = attributes_for(:vendor2)
  #   flag, _ = Vendor::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
