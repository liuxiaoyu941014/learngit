require 'rails_helper'

RSpec.describe Delivery::Create, type: :model do

  # it 'for name' do
  #   record = create(:delivery)
  #   flag, new_record = Delivery::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:delivery)
  #   record2 = create(:delivery2)
  #   attrs = attributes_for(:delivery2)
  #   flag, _ = Delivery::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
