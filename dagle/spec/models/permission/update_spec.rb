require 'rails_helper'

RSpec.describe Permission::Create, type: :model do

  # it 'for name' do
  #   record = create(:permission)
  #   flag, new_record = Permission::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:permission)
  #   record2 = create(:permission2)
  #   attrs = attributes_for(:permission2)
  #   flag, _ = Permission::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
