require 'rails_helper'

RSpec.describe MaterialWarehouseItem::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_warehouse_item)
  #   flag, new_record = MaterialWarehouseItem::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_warehouse_item)
  #   record2 = create(:material_warehouse_item2)
  #   attrs = attributes_for(:material_warehouse_item2)
  #   flag, _ = MaterialWarehouseItem::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
