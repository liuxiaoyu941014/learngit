require 'rails_helper'

RSpec.describe Article::Create, type: :model do

  # it 'for name' do
  #   record = create(:article)
  #   flag, new_record = Article::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:article)
  #   record2 = create(:article2)
  #   attrs = attributes_for(:article2)
  #   flag, _ = Article::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
