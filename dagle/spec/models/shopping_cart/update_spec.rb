require 'rails_helper'

RSpec.describe ShoppingCart::Create, type: :model do

  # it 'for name' do
  #   record = create(:shopping_cart)
  #   flag, new_record = ShoppingCart::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:shopping_cart)
  #   record2 = create(:shopping_cart2)
  #   attrs = attributes_for(:shopping_cart2)
  #   flag, _ = ShoppingCart::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
