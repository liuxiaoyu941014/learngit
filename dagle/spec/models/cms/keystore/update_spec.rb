require 'rails_helper'

RSpec.describe Cms::Keystore::Create, type: :model do

  # it 'for name' do
  #   record = create(:keystore)
  #   flag, new_record = Cms::Keystore::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:keystore)
  #   record2 = create(:keystore2)
  #   attrs = attributes_for(:keystore2)
  #   flag, _ = Cms::Keystore::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
