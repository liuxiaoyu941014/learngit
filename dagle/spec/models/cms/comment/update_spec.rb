require 'rails_helper'

RSpec.describe Cms::Comment::Create, type: :model do

  # it 'for name' do
  #   record = create(:comment)
  #   flag, new_record = Cms::Comment::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:comment)
  #   record2 = create(:comment2)
  #   attrs = attributes_for(:comment2)
  #   flag, _ = Cms::Comment::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
