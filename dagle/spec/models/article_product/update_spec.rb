require 'rails_helper'

RSpec.describe ArticleProduct::Create, type: :model do

  # it 'for name' do
  #   record = create(:article_product)
  #   flag, new_record = ArticleProduct::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:article_product)
  #   record2 = create(:article_product2)
  #   attrs = attributes_for(:article_product2)
  #   flag, _ = ArticleProduct::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
