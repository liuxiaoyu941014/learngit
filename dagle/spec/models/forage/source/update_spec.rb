require 'rails_helper'

RSpec.describe Forage::Source::Create, type: :model do

  # it 'for name' do
  #   record = create(:source)
  #   flag, new_record = Forage::Source::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:source)
  #   record2 = create(:source2)
  #   attrs = attributes_for(:source2)
  #   flag, _ = Forage::Source::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
