require 'rails_helper'

RSpec.describe Forage::Detail::Create, type: :model do

  # it 'for name' do
  #   record = create(:detail)
  #   flag, new_record = Forage::Detail::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:detail)
  #   record2 = create(:detail2)
  #   attrs = attributes_for(:detail2)
  #   flag, _ = Forage::Detail::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
