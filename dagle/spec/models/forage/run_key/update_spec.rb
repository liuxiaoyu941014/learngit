require 'rails_helper'

RSpec.describe Forage::RunKey::Create, type: :model do

  # it 'for name' do
  #   record = create(:run_key)
  #   flag, new_record = Forage::RunKey::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:run_key)
  #   record2 = create(:run_key2)
  #   attrs = attributes_for(:run_key2)
  #   flag, _ = Forage::RunKey::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
