require 'rails_helper'

RSpec.describe Refund::Create, type: :model do

  # it 'for name' do
  #   record = create(:refund)
  #   flag, new_record = Refund::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:refund)
  #   record2 = create(:refund2)
  #   attrs = attributes_for(:refund2)
  #   flag, _ = Refund::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
