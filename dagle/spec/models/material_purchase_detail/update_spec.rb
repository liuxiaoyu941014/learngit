require 'rails_helper'

RSpec.describe MaterialPurchaseDetail::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_purchase_detail)
  #   flag, new_record = MaterialPurchaseDetail::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_purchase_detail)
  #   record2 = create(:material_purchase_detail2)
  #   attrs = attributes_for(:material_purchase_detail2)
  #   flag, _ = MaterialPurchaseDetail::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
