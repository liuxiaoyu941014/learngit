require 'rails_helper'

RSpec.describe Catalog::Create, type: :model do

  # it 'for name' do
  #   record = create(:catalog)
  #   flag, new_record = Catalog::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:catalog)
  #   record2 = create(:catalog2)
  #   attrs = attributes_for(:catalog2)
  #   flag, _ = Catalog::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
