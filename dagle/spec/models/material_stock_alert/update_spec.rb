require 'rails_helper'

RSpec.describe MaterialStockAlert::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_stock_alert)
  #   flag, new_record = MaterialStockAlert::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_stock_alert)
  #   record2 = create(:material_stock_alert2)
  #   attrs = attributes_for(:material_stock_alert2)
  #   flag, _ = MaterialStockAlert::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
