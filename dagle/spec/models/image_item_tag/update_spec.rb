require 'rails_helper'

RSpec.describe ImageItemTag::Create, type: :model do

  # it 'for name' do
  #   record = create(:image_item_tag)
  #   flag, new_record = ImageItemTag::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:image_item_tag)
  #   record2 = create(:image_item_tag2)
  #   attrs = attributes_for(:image_item_tag2)
  #   flag, _ = ImageItemTag::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
