require 'rails_helper'

RSpec.describe MarketCatalog::Create, type: :model do

  # it 'for name' do
  #   record = create(:market_catalog)
  #   flag, new_record = MarketCatalog::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:market_catalog)
  #   record2 = create(:market_catalog2)
  #   attrs = attributes_for(:market_catalog2)
  #   flag, _ = MarketCatalog::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
