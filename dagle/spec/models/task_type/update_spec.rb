require 'rails_helper'

RSpec.describe TaskType::Create, type: :model do

  # it 'for name' do
  #   record = create(:task_type)
  #   flag, new_record = TaskType::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:task_type)
  #   record2 = create(:task_type2)
  #   attrs = attributes_for(:task_type2)
  #   flag, _ = TaskType::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
