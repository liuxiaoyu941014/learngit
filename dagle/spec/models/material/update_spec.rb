require 'rails_helper'

RSpec.describe Material::Create, type: :model do

  # it 'for name' do
  #   record = create(:material)
  #   flag, new_record = Material::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material)
  #   record2 = create(:material2)
  #   attrs = attributes_for(:material2)
  #   flag, _ = Material::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
