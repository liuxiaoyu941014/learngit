require 'rails_helper'

RSpec.describe Logistic::Create, type: :model do

  # it 'for name' do
  #   record = create(:logistic)
  #   flag, new_record = Logistic::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:logistic)
  #   record2 = create(:logistic2)
  #   attrs = attributes_for(:logistic2)
  #   flag, _ = Logistic::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
