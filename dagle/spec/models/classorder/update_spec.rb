require 'rails_helper'

RSpec.describe Classorder::Create, type: :model do

  # it 'for name' do
  #   record = create(:classorder)
  #   flag, new_record = Classorder::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:classorder)
  #   record2 = create(:classorder2)
  #   attrs = attributes_for(:classorder2)
  #   flag, _ = Classorder::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
