require 'rails_helper'

RSpec.describe Member::Create, type: :model do

  # it 'for name' do
  #   record = create(:member)
  #   flag, new_record = Member::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:member)
  #   record2 = create(:member2)
  #   attrs = attributes_for(:member2)
  #   flag, _ = Member::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
