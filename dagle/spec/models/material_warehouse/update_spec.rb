require 'rails_helper'

RSpec.describe MaterialWarehouse::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_warehouse)
  #   flag, new_record = MaterialWarehouse::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_warehouse)
  #   record2 = create(:material_warehouse2)
  #   attrs = attributes_for(:material_warehouse2)
  #   flag, _ = MaterialWarehouse::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
