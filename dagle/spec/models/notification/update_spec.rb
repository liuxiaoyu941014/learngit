require 'rails_helper'

RSpec.describe Notification::Create, type: :model do

  # it 'for name' do
  #   record = create(:notification)
  #   flag, new_record = Notification::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:notification)
  #   record2 = create(:notification2)
  #   attrs = attributes_for(:notification2)
  #   flag, _ = Notification::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
