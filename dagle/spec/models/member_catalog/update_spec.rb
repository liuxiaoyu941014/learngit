require 'rails_helper'

RSpec.describe MemberCatalog::Create, type: :model do

  # it 'for name' do
  #   record = create(:member_catalog)
  #   flag, new_record = MemberCatalog::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:member_catalog)
  #   record2 = create(:member_catalog2)
  #   attrs = attributes_for(:member_catalog2)
  #   flag, _ = MemberCatalog::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
