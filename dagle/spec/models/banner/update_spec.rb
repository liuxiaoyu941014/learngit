require 'rails_helper'

RSpec.describe Banner::Create, type: :model do

  # it 'for name' do
  #   record = create(:banner)
  #   flag, new_record = Banner::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:banner)
  #   record2 = create(:banner2)
  #   attrs = attributes_for(:banner2)
  #   flag, _ = Banner::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
