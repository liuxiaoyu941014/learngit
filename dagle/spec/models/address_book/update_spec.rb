require 'rails_helper'

RSpec.describe AddressBook::Create, type: :model do

  # it 'for name' do
  #   record = create(:address_book)
  #   flag, new_record = AddressBook::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:address_book)
  #   record2 = create(:address_book2)
  #   attrs = attributes_for(:address_book2)
  #   flag, _ = AddressBook::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
