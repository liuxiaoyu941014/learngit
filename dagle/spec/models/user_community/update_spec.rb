require 'rails_helper'

RSpec.describe UserCommunity::Create, type: :model do

  # it 'for name' do
  #   record = create(:user_community)
  #   flag, new_record = UserCommunity::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:user_community)
  #   record2 = create(:user_community2)
  #   attrs = attributes_for(:user_community2)
  #   flag, _ = UserCommunity::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
