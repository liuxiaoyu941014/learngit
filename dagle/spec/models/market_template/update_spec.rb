require 'rails_helper'

RSpec.describe MarketTemplate::Create, type: :model do

  # it 'for name' do
  #   record = create(:market_template)
  #   flag, new_record = MarketTemplate::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:market_template)
  #   record2 = create(:market_template2)
  #   attrs = attributes_for(:market_template2)
  #   flag, _ = MarketTemplate::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
