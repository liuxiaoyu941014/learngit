require 'rails_helper'

RSpec.describe MarketPage::Create, type: :model do

  # it 'for name' do
  #   record = create(:market_page)
  #   flag, new_record = MarketPage::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:market_page)
  #   record2 = create(:market_page2)
  #   attrs = attributes_for(:market_page2)
  #   flag, _ = MarketPage::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
