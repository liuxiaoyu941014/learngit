require 'rails_helper'

RSpec.describe AccountHistory::Create, type: :model do

  # it 'for name' do
  #   record = create(:account_history)
  #   flag, new_record = AccountHistory::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:account_history)
  #   record2 = create(:account_history2)
  #   attrs = attributes_for(:account_history2)
  #   flag, _ = AccountHistory::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
