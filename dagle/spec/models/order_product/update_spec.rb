require 'rails_helper'

RSpec.describe OrderProduct::Create, type: :model do

  # it 'for name' do
  #   record = create(:order_product)
  #   flag, new_record = OrderProduct::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:order_product)
  #   record2 = create(:order_product2)
  #   attrs = attributes_for(:order_product2)
  #   flag, _ = OrderProduct::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
