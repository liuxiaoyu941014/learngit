require 'rails_helper'

RSpec.describe Course::Create, type: :model do

  # it 'for name' do
  #   record = create(:course)
  #   flag, new_record = Course::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:course)
  #   record2 = create(:course2)
  #   attrs = attributes_for(:course2)
  #   flag, _ = Course::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
