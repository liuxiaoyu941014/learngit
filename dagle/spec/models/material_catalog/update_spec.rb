require 'rails_helper'

RSpec.describe MaterialCatalog::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_catalog)
  #   flag, new_record = MaterialCatalog::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_catalog)
  #   record2 = create(:material_catalog2)
  #   attrs = attributes_for(:material_catalog2)
  #   flag, _ = MaterialCatalog::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
