require 'rails_helper'

RSpec.describe OrderMaterial::Create, type: :model do

  # it 'for name' do
  #   record = create(:order_material)
  #   flag, new_record = OrderMaterial::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:order_material)
  #   record2 = create(:order_material2)
  #   attrs = attributes_for(:order_material2)
  #   flag, _ = OrderMaterial::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
