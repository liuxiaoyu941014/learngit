require 'rails_helper'

RSpec.describe Order::Create, type: :model do

  # it 'for name' do
  #   record = create(:order)
  #   flag, new_record = Order::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:order)
  #   record2 = create(:order2)
  #   attrs = attributes_for(:order2)
  #   flag, _ = Order::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
