require 'rails_helper'

RSpec.describe FinanceBill::Create, type: :model do

  # it 'for name' do
  #   record = create(:finance_bill)
  #   flag, new_record = FinanceBill::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:finance_bill)
  #   record2 = create(:finance_bill2)
  #   attrs = attributes_for(:finance_bill2)
  #   flag, _ = FinanceBill::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
