require 'rails_helper'

RSpec.describe Chat::Message::Create, type: :model do

  # it 'for name' do
  #   record = create(:message)
  #   flag, new_record = Chat::Message::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:message)
  #   record2 = create(:message2)
  #   attrs = attributes_for(:message2)
  #   flag, _ = Chat::Message::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
