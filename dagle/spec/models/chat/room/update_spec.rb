require 'rails_helper'

RSpec.describe Chat::Room::Create, type: :model do

  # it 'for name' do
  #   record = create(:room)
  #   flag, new_record = Chat::Room::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:room)
  #   record2 = create(:room2)
  #   attrs = attributes_for(:room2)
  #   flag, _ = Chat::Room::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
