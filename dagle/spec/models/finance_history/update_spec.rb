require 'rails_helper'

RSpec.describe FinanceHistory::Create, type: :model do

  # it 'for name' do
  #   record = create(:finance_history)
  #   flag, new_record = FinanceHistory::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:finance_history)
  #   record2 = create(:finance_history2)
  #   attrs = attributes_for(:finance_history2)
  #   flag, _ = FinanceHistory::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
