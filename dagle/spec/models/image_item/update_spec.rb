require 'rails_helper'

RSpec.describe ImageItem::Create, type: :model do

  # it 'for name' do
  #   record = create(:image_item)
  #   flag, new_record = ImageItem::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:image_item)
  #   record2 = create(:image_item2)
  #   attrs = attributes_for(:image_item2)
  #   flag, _ = ImageItem::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
