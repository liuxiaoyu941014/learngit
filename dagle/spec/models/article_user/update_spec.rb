require 'rails_helper'

RSpec.describe ArticleUser::Create, type: :model do

  # it 'for name' do
  #   record = create(:article_user)
  #   flag, new_record = ArticleUser::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:article_user)
  #   record2 = create(:article_user2)
  #   attrs = attributes_for(:article_user2)
  #   flag, _ = ArticleUser::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
