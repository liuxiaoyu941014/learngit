require 'rails_helper'

RSpec.describe MemberNote::Create, type: :model do

  # it 'for name' do
  #   record = create(:member_note)
  #   flag, new_record = MemberNote::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:member_note)
  #   record2 = create(:member_note2)
  #   attrs = attributes_for(:member_note2)
  #   flag, _ = MemberNote::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
