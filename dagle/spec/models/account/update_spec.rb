require 'rails_helper'

RSpec.describe Account::Create, type: :model do

  # it 'for name' do
  #   record = create(:account)
  #   flag, new_record = Account::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:account)
  #   record2 = create(:account2)
  #   attrs = attributes_for(:account2)
  #   flag, _ = Account::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
