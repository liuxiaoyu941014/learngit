require 'rails_helper'

RSpec.describe Diymenu::Create, type: :model do

  # it 'for name' do
  #   record = create(:diymenu)
  #   flag, new_record = Diymenu::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:diymenu)
  #   record2 = create(:diymenu2)
  #   attrs = attributes_for(:diymenu2)
  #   flag, _ = Diymenu::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
