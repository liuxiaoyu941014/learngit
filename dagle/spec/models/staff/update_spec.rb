require 'rails_helper'

RSpec.describe Staff::Create, type: :model do

  # it 'for name' do
  #   record = create(:staff)
  #   flag, new_record = Staff::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:staff)
  #   record2 = create(:staff2)
  #   attrs = attributes_for(:staff2)
  #   flag, _ = Staff::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
