require 'rails_helper'

RSpec.describe Task::Create, type: :model do

  # it 'for name' do
  #   record = create(:task)
  #   flag, new_record = Task::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:task)
  #   record2 = create(:task2)
  #   attrs = attributes_for(:task2)
  #   flag, _ = Task::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
