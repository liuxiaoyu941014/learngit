require 'rails_helper'

RSpec.describe Charge::Create, type: :model do

  # it 'for name' do
  #   record = create(:charge)
  #   flag, new_record = Charge::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:charge)
  #   record2 = create(:charge2)
  #   attrs = attributes_for(:charge2)
  #   flag, _ = Charge::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
