require 'rails_helper'

RSpec.describe MaterialManagement::Create, type: :model do

  # it 'for name' do
  #   record = create(:material_management)
  #   flag, new_record = MaterialManagement::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:material_management)
  #   record2 = create(:material_management2)
  #   attrs = attributes_for(:material_management2)
  #   flag, _ = MaterialManagement::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
