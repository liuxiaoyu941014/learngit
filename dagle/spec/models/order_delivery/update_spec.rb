require 'rails_helper'

RSpec.describe OrderDelivery::Create, type: :model do

  # it 'for name' do
  #   record = create(:order_delivery)
  #   flag, new_record = OrderDelivery::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:order_delivery)
  #   record2 = create(:order_delivery2)
  #   attrs = attributes_for(:order_delivery2)
  #   flag, _ = OrderDelivery::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
