require 'rails_helper'

RSpec.describe Produce::Create, type: :model do

  # it 'for name' do
  #   record = create(:produce)
  #   flag, new_record = Produce::Update.(record, name: 'new name')
  #   expect(flag).to eq(true)
  #   expect(new_record.name).to eq(new_record.name)
  # end

  # it 'with existing record' do
  #   record1 = create(:produce)
  #   record2 = create(:produce2)
  #   attrs = attributes_for(:produce2)
  #   flag, _ = Produce::Update.(record1, attrs)
  #   expect(flag).to eq(false)
  # end

  pending "add some examples to (or delete) #{__FILE__}"
end
