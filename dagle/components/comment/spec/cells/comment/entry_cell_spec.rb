require 'rails_helper'
RSpec.describe Comment::EntryCell do
  # Cell::Testing.capybara = true
  # controller PostsController
  # let(:post_record) { create(:post) }
  # subject { cell(described_class, post_record, path: comments_post_path(post_record)).() }
  
  # it do
  #   post_record.comments.create content: 'comment 01'
  #   post_record.comments.create content: 'comment 02'
  #   expect(subject).to have_selector '.comment'

  #   expect(subject).to have_content "comment 01"
  #   expect(subject).to have_content "comment 02"

  # end
end
