---
zh-CN:
  activerecord:
    models:
      keystore: 系统参数
    attributes:
      keystore:
        key: 键(Key)
        value: 值(Value)
        description: 描述
    errors:
      models:
        keystore:
  helpers:
    keystore:
