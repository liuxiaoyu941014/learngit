.container
  h1
    | 编辑基本信息
    .pull-right
      => link_to '查看', agent_site_path(@agent_site), class: 'btn btn-primary'
      => link_to '返回', agent_root_path, class: 'btn btn-default'

  == render 'form'
